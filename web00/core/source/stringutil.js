module.exports = {
	//we don't NEED to deepcopy strings?
	// "deepCopyString" : function(in_originalString){
	// 	const stringCopy = (' ' + in_originalString).slice(1);
	// 	return stringCopy;
	// }
}
