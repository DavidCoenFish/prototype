todo:
_	heightmap float test
	celtic knot tiles
	heightmap & envmap
	heightmap & light, shadows
	fade in/out, drop shadows
	blend over time
	performance test sphere octree

x done
_ underway

npm init -y
npm pack

Float32Array
Int8Array
Uint8Array
Int16Array
Uint16Array
Int32Array
Uint32Array

moe
0G8006T10600

for (var index = 0; index < cars.length; index++) { 
    text += cars[index] + "<br>";
}

for (var key in object) {
	if (object.hasOwnProperty(key)) callback(key, object[key]);
}
